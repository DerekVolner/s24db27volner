extends layout

block content
  h1= title
  -  let items = results
  div.container-fluid 
    div.row
      each item in items
        div.col-sm-2 #{item.rhino_species} #{ item.rhino_age} #{item.endangerment_status}

